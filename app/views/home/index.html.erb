<%= render partial: "shared/top_bar", locals: {
  session: @session,
} %>

<div class="d-flex h-100 justify-content-around mt-5">
  <div class="col-md-2 h-100">
    <%= render partial: "shared/side_navigation_bar", locals: { session: @session } %>
  </div>
  <div class="col-md-9 h-100">
  </div>
</div>
